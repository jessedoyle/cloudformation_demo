source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.1.0'
gem 'rails', '~> 7.0.2'
gem 'pg'
gem 'puma'
gem 'rest-client'

group :development do
  gem 'pry'
end
