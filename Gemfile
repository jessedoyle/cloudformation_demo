source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.3.5'
gem 'pg'
gem 'puma'
gem 'rails', '~> 7.2'
gem 'rest-client'

group :development do
  gem 'pry'
end
